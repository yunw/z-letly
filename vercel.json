{
  "version": 2,
  "buildCommand": "npm run build:frontend",
  "installCommand": "npm run install:all",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/frontend/dist/$1" }
  ],
  "functions": {
    "api/**/*.js": {
      "maxDuration": 30
    }
  }
} 
